# nixos-configuration
My Nix configs

## TODOs
add git configuration
nix settings and tooling - like alejandra - also add pre-commit hook
proper emacs setup - with server and only starting clients - also nix dev packages and stuff
terminal - alacritty setup
xmonad setup - includes a service file for all the bull that needs to run alongside it
